module creature;

import entity;

/* Creatures are the actors in the world. Every living (or undead) thing is
 * a creature.
 * Creatures can:
 * - have behaviorial patterns
 * - find paths
 * - move themselves
 * - pickup items
 * - move items (with them, into containers, from containers)
 * - die
 * - attack (using items)
 * - have a body
 */
